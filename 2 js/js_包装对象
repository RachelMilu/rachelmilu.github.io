今天继续学习JavaScript之包装对象，
原文在此：http://www.html5cn.org/article-8387-1.html
接下来是摘抄和理解：
js的五种原始类型string，number，boolean，undefined，null中，前三种都有属于自己的封装对象。
当你用var来声明和赋值前三种类型的数据时，JavaScript就会通过调用封装对象的构造器来构造一个临时对象。
此时可以使用封装对象的方法和属性。但是由于这些被构造出来的对象是临时的，对他们的方法和属性进行修改将不会被保存。

“JavaScript会在必要时将包装对象转换成原始值。
"=="等于运算符将原始值和其包装对象视为相等，但“===”全等运算将它们视为不等。
通过typeof运算符也可以看到原始值和其包装对象的不同：”
