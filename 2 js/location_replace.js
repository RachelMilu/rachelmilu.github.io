 //var currLocation = location.href;
        //var target = "";
        //
        //if (gIdReg.test(location)) {
        //    target = location.search.replace(gIdReg, function (a, b, c, d) {
        //        return b + c + gid;
        //    });
        //} else {
        //    target = currLocation.indexOf('?') > -1 ? (currLocation + "&g=" + gid) : (currLocation + "?g=" + gid);
        //}
        //
        //location.href = target;
